diff --git a/dist/cli.mjs b/dist/cli.mjs
index 07ae3c67852b22504ae04f8f507c55284f3e8b26..69c3419169313381423b3490f8ea558617d69826 100644
--- a/dist/cli.mjs
+++ b/dist/cli.mjs
@@ -12,7 +12,6 @@ import childProcess, { execFile } from "node:child_process";
 import { format, inspect, promisify } from "node:util";
 import { Buffer as Buffer$1 } from "node:buffer";
 import { glob } from "tinyglobby";
-import { bundleRequire } from "bundle-require";
 import assert from "node:assert";
 import v8 from "node:v8";
 import tty from "node:tty";
@@ -20,6 +19,7 @@ import { createServer as createServer$1 } from "node:http";
 import { createApp, eventHandler, serveStatic, toNodeListener } from "h3";
 import chokidar from "chokidar";
 import { WebSocketServer } from "ws";
+import { createJiti } from 'jiti'
 
 //#region \0rolldown/runtime.js
 var __create = Object.create;
@@ -14583,12 +14583,9 @@ async function readConfig(options) {
 	const { basePath, configPath } = await resolveConfigPath(options);
 	if (chdir && basePath !== process$1.cwd()) process$1.chdir(basePath);
 	console.log(MARK_INFO, `Reading ESLint config from`, c.blue(configPath));
-	const { mod, dependencies } = await bundleRequire({
-		filepath: configPath,
-		cwd: basePath,
-		tsconfig: false
-	});
-	let rawConfigs = await (mod.default ?? mod);
+	const jiti = createJiti(configPath);
+  let rawConfigs = await jiti.import(configPath, { default: true });
+  const dependencies = Object.keys(jiti.cache).map(i => i.replace(/\\/g, '/')).filter(i => !i.includes('/node_modules/'));
 	if (!Array.isArray(rawConfigs)) rawConfigs = [rawConfigs];
 	rawConfigs.unshift({
 		index: 1,
